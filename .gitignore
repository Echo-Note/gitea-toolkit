node_modules/
dist/
out/
*.vsix
*.tgz
.DS_Store
.env
.env.local

# 凭证类文件 —— 绝不要提交。
# 起因：仓库根目录出现过一个 apikey 文件（45 字节的明文密钥），既没被忽略、
# 也没进过历史，而本项目的提交流程用 `git add -A`，下一次提交就会把它推到公开仓库。
apikey
apikey.*
secrets.json
*.pem
*.key

# Python（python/ 独立包）
# 起因：首次提交 Python 版时，`git add -A` 把 __pycache__/*.pyc 一起提交了 —— 构建产物不入库。
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
dist/
build/
*.log
.eslintcache

# 独立 MCP 包的构建产物（dist 已由上面的 dist/ 覆盖，这里是随包拷贝的协议文本）
packages/mcp-server/LICENSE
packages/mcp-server/dist/

# AI 协作工作记忆：含本机绝对路径与内部实例信息，仅本地保留
.codebuddy/memory/

